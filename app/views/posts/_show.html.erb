<a href=<%= show_post_path(id: post.id) %>>
	<p><%= post.title %></p>
</a>
<p><%= post.author %></p>
<p><%= post.body.html_safe %></p>
<% if edit == true %>
	<%= link_to 'edit', edit_post_path(post_id: post.id) %>
<% end %>
