<h1>Editing "<%= @post.title %>"</h1>


<%= form_for @post, url: {action: "update_post"} do |f| %>
  <p><%= f.text_field :title, class: "form-control" %></p>
  <p><%= f.text_area :body, class: "form-control", rows: 14 %></p>
  <%= f.hidden_field :id %>
  <%= f.submit "Update" %>
<% end %>