<!-- New Post Here -->
<br>
<%= link_to "New Post", admin_new_post_path %>
<br>
<br>

<!-- Editable posts -->
<div class="row">
	<% @posts.each do |post| %>
		<% if post.author == username(current_admin.email) %>
			<%= render 'posts/post', post: post %>
		<% end %>
	<% end %>
</div>